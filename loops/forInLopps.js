/*
-for in loops are gret for iterating ovefr values in an object.
Properties in object are what's called enumberalbe

-for in loops iterate over an object's enumberable properties
*/
/*
let student = {
    name: "peter",
    awesome: true,
    degree: "JavaScript",
    week: 1
}

for (item in student) {
    console.log(item);
    //console.log (student [item]);

}
*/

//challenge: write a for in loop that capitalizes the first letter of a name, 
//and lowercases the rest of the name

/*
let myName = "dOnOvAN"
let capName;


for (let i in myName) {
    if(i == 0){
        capName = myName[i].toUpperCase();
    } else {
        capName = myName[i].toLowerCase();

    }
    console.log(capName);
*/    
    


